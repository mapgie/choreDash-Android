name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: mapgie/android-Actions/.github/workflows/release.yml@95e7e8181942991d22128cd5d12bb8027bcdb90a
    with:
      app-name: Dash
