{
  "bump": "minor",
  "added": [
    "Display settings sub-screen with toggles for grouping chores and tasks by category, and configurable per-list thresholds to move items not due soon into a collapsible distant section",
    "Tasks now have a collapsible distant section (matching the existing chores mechanic) when a hide-threshold is configured"
  ],
  "changed": [
    "Group-by-category toggle moved from the chores and tasks toolbars into Settings > Display; the setting is now persisted across sessions",
    "Chore distant threshold is now configurable in Settings > Display instead of being hard-coded at 60 days; the 60-day default is preserved when no threshold is set",
    "Zen mode lotus icon enlarged for easier tapping"
  ]
}
